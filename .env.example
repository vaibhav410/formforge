# Root env consumed by docker-compose.yml variable substitution.
# Copy to .env, then:
#   APP_KEY:  cd web && php artisan key:generate --show
#   AI_SERVICE_TOKEN: any long random hex; must match ai-service/.env
APP_KEY=
AI_SERVICE_TOKEN=change-me-shared-secret
